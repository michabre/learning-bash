# Web Stuff

## Commands

```shell
# start a web server
python3 -m http.server

# curl
curl <url>

# save contents of url to a file
curl -o <local file> <url>

#  get response header
curl -I <url>

# include request header
curl -v <url>

# download a file
wget <url>




```
